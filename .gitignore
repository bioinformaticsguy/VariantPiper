# pipeline inputs/outputs (too large for git)
/input/
/output/

# downloaded resources (reference genome, indexes, singularity images, delly files)
/resources/reference/
/resources/singularity/
/resources/delly/
# keep the README
!/resources/README.md

# snakemake internals
.snakemake/

# python
__pycache__/
*.pyc

# ignore Claude
CLAUDE.md


# ignore logs\
/logs

# ignore the rare disease pipeline
/raredisease-2.6.0.zip

# production samplesheets contain real data paths — keep out of git
# (exclude every file inside samplesheets/ so negations below work)
/samplesheets/*
# but check in test samplesheets (no sensitive data)
!/samplesheets/test*.tsv