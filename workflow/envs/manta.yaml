channels:
  - conda-forge
  - bioconda
dependencies:
  - manta=1.6.0
  - python=2.7      # manta scripts use #!/usr/bin/env python2; conda creates python2 symlink when python=2.7 is pinned
  - bcftools>=1.17
  - libdeflate      # runtime dependency of bcftools (libdeflate.so)
